---
description: "前端开发规范 skill 适配入口。用于 React、Vue、TypeScript、Electron 渲染进程、H5、后台管理系统等前端项目开发、修改、重构时遵循统一工程规范。"
alwaysApply: true
---

# Frontend Rule Skill

本仓库包含 `frontend-rule` skill。

Trae 使用本规则时，请读取并遵循核心规则文件：

```text
skills/frontend-rule/SKILL.md
```

适用场景：React、Vue、TypeScript、Electron 渲染进程、H5、后台管理系统等前端项目的编写、修改、重构和代码审查。

触发关键词：`frontend-rule`、`$frontend-rule`、前端开发规范、项目目录规范、路由 name 规范、魔法值治理、枚举常量、工程化规范。

维护原则：本文件只是 Trae 识别入口，核心规则只维护在 `skills/frontend-rule/SKILL.md`。如果本文件与核心规则冲突，以 `skills/frontend-rule/SKILL.md` 为准。
