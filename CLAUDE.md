# Frontend Rule Skill

This repository contains the `frontend-rule` skill.

When working on frontend projects, read and follow the canonical rules in:

```text
skills/frontend-rule/SKILL.md
```

Use these rules for React, Vue, TypeScript, Electron renderer, H5, and admin-system code changes. The rules cover directory responsibilities, naming, typing, API boundaries, state management, routing, styling, constants/enums, engineering review, and verification.

If the user mentions `frontend-rule`, `$frontend-rule`, front-end development rules, project structure rules, route naming rules, magic-value cleanup, or engineering standards, treat `skills/frontend-rule/SKILL.md` as the source of truth.

Do not duplicate or override the skill content here. If this file and `skills/frontend-rule/SKILL.md` conflict, follow `skills/frontend-rule/SKILL.md`.
